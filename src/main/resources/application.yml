server:
  port: 8080

app:
  api:
    name: Cupon de Compra
    version: v1

spring:
  datasource:
    url: jdbc:postgresql://db-postgresql-sfo2-30825-do-user-11392574-0.b.db.ondigitalocean.com:25060/defaultdb
    driverClassName: org.postgresql.Driver
    username: doadmin
    password: AVNS_DDYMeXrgKs0dpZI
#  h2:
#    console:
#      enabled: true
#  datasource:
#    url: jdbc:h2:mem:coupondb
#    driverClassName: org.h2.Driver
#    username: sa
#    password:

  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

management:
  endpoint:
    health:
      probes:
        enabled: true
      show-details: ALWAYS

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs